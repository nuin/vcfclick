"""Native variant concordance & benchmarking (`vcfclick benchmark`).

File-based, SQL-free P1 (`--engine normalized`). See docs; this is *not*
hap.py — its INDEL numbers are not hap.py-comparable.
"""

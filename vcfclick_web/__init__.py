"""Optional local web UI for vcfclick (`vcfclick web <db>`).

Installed via the `[web]` extra. Serves a localhost single-page app over a
cohort database: a SQL explorer, a natural-language→SQL box, and trio /
combine panels. Everything runs on the user's machine against the
embedded backend — there is no hosted service.
"""

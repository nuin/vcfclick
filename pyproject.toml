[project]
name = "vcfclick"
version = "0.7.0"
description = "Small VCF databases. One per cohort. Embedded ClickHouse engine, embedded DuckDB annotations, MCP natural-language layer."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "nuin", email = "nuin@genedrift.org" },
]
keywords = [
    "vcf",
    "bioinformatics",
    "genomics",
    "clickhouse",
    "duckdb",
    "embedded-database",
    "mcp",
    "model-context-protocol",
    "natural-language-sql",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Database",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "cyvcf2>=0.31.0",
    "chdb>=4.1.8",
    "pyarrow>=15.0.0",
    "duckdb>=1.0.0",
    "mcp>=1.0.0",
    "click>=8.1.0",
]

[project.optional-dependencies]
tui = [
    "textual>=0.89",
]
web = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.25.0",
    "pydantic>=2",
]

[project.urls]
Homepage = "https://github.com/nuin/vcfclick"
Repository = "https://github.com/nuin/vcfclick"
Issues = "https://github.com/nuin/vcfclick/issues"
Benchmark = "https://github.com/nuin/vcfclick/blob/main/bench/BENCHMARK.md"

[project.scripts]
vcfclick = "cli.main:cli"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.15",
    "httpx>=0.27",
    "pytest-rerunfailures>=14.0",
    "pytest-timeout>=2.3",
]

[tool.pytest.ini_options]
# chDB occasionally crashes OR hangs a query/ingest subprocess on CI
# (SIGABRT / SIGSEGV from glibc pthread_mutex / recursive_mutex
# assertions, or a multi-minute deadlock), a known embedded-engine flake
# unrelated to the code under test. --timeout caps any single test so a
# hang fails fast instead of stalling the whole job; --reruns then
# retries ONLY those native-crash / timeout signatures. A genuinely
# failing assertion fails all attempts and still reports, so retries
# rescue flakes without masking real failures. No real test needs 180s.
timeout = 180
addopts = "--reruns 2 --reruns-delay 2 --only-rerun rc=- --only-rerun returncode=- --only-rerun pthread_mutex --only-rerun recursive_mutex --only-rerun glibc --only-rerun Timeout"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "annotations",
    "cli",
    "export",
    "ingest",
    "schema",
    "storage",
    "tui",
    "vcfclick_mcp",
    "vcfclick_web",
]

[tool.hatch.build.targets.sdist]
include = [
    "annotations",
    "cli",
    "export",
    "ingest",
    "schema",
    "storage",
    "tui",
    "vcfclick_mcp",
    "bench/BENCHMARK.md",
    "bench/run.sh",
    "examples",
    "LICENSE",
    "LICENSING.md",
    "README.md",
    "pyproject.toml",
]

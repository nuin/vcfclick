[project]
name = "vcfclick"
version = "0.6.0"
description = "Small VCF databases. One per cohort. Embedded ClickHouse engine, embedded DuckDB annotations, MCP natural-language layer."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "nuin", email = "nuin@genedrift.org" },
]
keywords = [
    "vcf",
    "bioinformatics",
    "genomics",
    "clickhouse",
    "duckdb",
    "embedded-database",
    "mcp",
    "model-context-protocol",
    "natural-language-sql",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Database",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "cyvcf2>=0.31.0",
    "chdb>=4.1.8",
    "pyarrow>=15.0.0",
    "duckdb>=1.0.0",
    "mcp>=1.0.0",
    "click>=8.1.0",
]

[project.optional-dependencies]
tui = [
    "textual>=0.89",
]
web = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.25.0",
    "pydantic>=2",
]

[project.urls]
Homepage = "https://github.com/nuin/vcfclick"
Repository = "https://github.com/nuin/vcfclick"
Issues = "https://github.com/nuin/vcfclick/issues"
Benchmark = "https://github.com/nuin/vcfclick/blob/main/bench/BENCHMARK.md"

[project.scripts]
vcfclick = "cli.main:cli"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.15",
    "httpx>=0.27",
    "pytest-rerunfailures>=14.0",
]

[tool.pytest.ini_options]
# chDB occasionally crashes a query/ingest subprocess on CI (SIGABRT /
# SIGSEGV — glibc pthread_mutex / recursive_mutex assertions), a known
# embedded-engine flake unrelated to the code under test. Auto-retry ONLY
# those native-crash signatures: a genuinely failing assertion fails all
# attempts and still reports, so retries rescue flakes without masking
# real failures.
addopts = "--reruns 2 --reruns-delay 2 --only-rerun rc=- --only-rerun returncode=- --only-rerun pthread_mutex --only-rerun recursive_mutex --only-rerun glibc"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "annotations",
    "cli",
    "export",
    "ingest",
    "schema",
    "storage",
    "tui",
    "vcfclick_mcp",
    "vcfclick_web",
]

[tool.hatch.build.targets.sdist]
include = [
    "annotations",
    "cli",
    "export",
    "ingest",
    "schema",
    "storage",
    "tui",
    "vcfclick_mcp",
    "bench/BENCHMARK.md",
    "bench/run.sh",
    "examples",
    "LICENSE",
    "LICENSING.md",
    "README.md",
    "pyproject.toml",
]
